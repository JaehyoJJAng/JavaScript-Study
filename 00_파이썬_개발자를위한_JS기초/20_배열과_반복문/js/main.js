let dataTypes = ['number', 'string', 'boolean', 'null', 'undefined', 'array'];

// 여기에 코드를 작성하세요
{
  let forCount=0;
  for (
    forCount;
    forCount < dataTypes.length;
    forCount++
    ) {
      data = dataTypes[forCount];
      console.log(data);
  }
}